[phases.setup]
nixPkgs = ["nodejs_24", "npm-10_x", "openssl"]

[variables]
NODE_VERSION = "24"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npx prisma generate"]

[start]
cmd = "npm start"

